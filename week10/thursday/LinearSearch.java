public class Main {
    public static void main(String[] args) {
        int[] arr = {1,5,9,3,2,4};

        int i = indexOf(arr, 9);
        // i should be 2
        int j = indexOf(arr, 3);
        // j should be 3
        int k = indexOf(arr, 99);
        // k should be -1, because not found
    }

    static int indexOf(int[] arr, int val) {

        return 0;
    }
}
